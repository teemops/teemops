MODE=dev
DOCKER_IMAGE_TAG=dev
#UI Variables
#This is the api endpoint. in local development you can use this. For everyhing else it will be the endpoint of the API Port 443.
API_URL=http://localhost:8080/api

#API Variables
#this is the port that the API will run on. Do not change otherwise docker will break.
PORT=8080
#these database credentials are for the docker container, password is generated on installation
DB_USER=teem
DB_PASS=
DB_HOST=mysql
DB_NAME=teem
DB_QUERY_TIMEOUT=10000

#AWS Account where teemops will be run from. Even when run from local, the AWS account must be set to the one where the resources are created.
AWS_ACCOUNT_ID=
#Generated by install
CONFIRM_SECRET=
SECRET=

#ignore STRIPE_KEY, this is for local development
STRIPE_KEY=sk_test_nothing

#if you want to add emails to the system, you can use the SMTP server of your choice. The default is maildev.
#The maildev is a container that is used to send emails. It is not a real SMTP server. It is used for local development.
#SMTP_HOST=maildev
SMTP_HOST=maildev
SMTP_USER=
SMTP_PASS=

MAILDEV_PORT_25_TCP_ADDR=maildev
MAILDEV_PORT_25_TCP_PORT=1025


#this is for local development, change for production - this is the UI endpoint base URL e.g. https://app.teemops.com
BASE_URL=http://localhost:3000

#S3 bucket and the config are generated by install
S3_KEY_STORE=
S3_APP_BUCKET=

#do not change
S3_APP_BUCKET_REGION=us-east-1
S3_APP_PATH=apps/vm/

#change this to the endpoint of the MFA service. This is used for MFA authentication.
#this is the default for local development
#MFA_AUTH_API=http://localhost:8787
MFA_AUTH_API=http://localhost:8787


# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

#change this to the endpoint of the database. This is used for database connection.
DATABASE_URL=mysql://teem:project@mysql:3306/teem


#NEVER NEVER EVER CHANGE THIS VALUE BELOW AFTER INSTALLATION OR ALL CHILD ACCOUNTS WILL STOP SENDING SNS MESSAGES
TOPS_SNS_ROOT_ACCOUNT_HASH=future_feature
